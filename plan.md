# Suika-style Game 実装計画 (plan.md)

このドキュメントは、AIアシスタント（Jules）が本ゲームを実装する際の手順を示すものです。
AIの出力制限（1回の回答で生成できるコード量）を考慮し、1つのステップが大きくなりすぎないように機能を分割しています。
各ステップ完了後には必ずブラウザでの動作確認を行い、正常に動作することを確認してから次のステップへ進みます。

## 技術スタック
- **言語**: HTML, CSS, JavaScript (ES6+)
- **物理エンジン**: Matter.js
- **環境**: スマートフォンブラウザ向け（GitHub Pages）

---

## Step 1: プロジェクトの初期設定と基本UI構築
**目的**: ゲームの土台となるHTML構造とCSSレイアウトを作成する。
- **実装内容**:
  - `index.html`, `style.css`, `main.js` のファイルを作成。
  - スマートフォン向けViewport設定（`width=device-width, initial-scale=1.0, user-scalable=no`）。
  - ゲームコンテナ、スコア表示エリア、Nextボール表示エリア、デッドライン線のUI作成。
- **作成・修正するファイル**: `index.html`, `style.css`
- **動作確認**:
  - ブラウザで画面が表示され、レイアウトが崩れていないか確認する。

## Step 2: 物理エンジン (Matter.js) の導入と環境構築
**目的**: Matter.jsを組み込み、物理演算の環境（重力、壁、床）を設定する。
- **実装内容**:
  - CDN経由で `matter.js` を読み込む。
  - `main.js` でMatter.jsのエンジン、レンダラーを初期化。
  - 画面左右の壁、底面の床を作成し、落下する物体が外に出ないようにする。
- **作成・修正するファイル**: `index.html`, `main.js`
- **動作確認**:
  - レンダラーによるキャンバスが画面に表示され、テスト用の四角形などを落として壁と床に衝突することを確認する。

## Step 3: ボール生成と落下処理の基本実装
**目的**: タップ操作によってランダムなボールを生成し、落下させる。
- **実装内容**:
  - 8種類のボール（サイズ、色、レベル）を定数として定義。
  - 落下する初期ボール（レベル1〜3の中からランダム）の決定ロジック作成。
  - 画面タップ（クリック/タッチ）で、その位置の真上からボールを生成・落下させるイベントリスナー追加。
- **作成・修正するファイル**: `main.js`
- **動作確認**:
  - 画面をタップするとランダムな大きさ・色のボールが落下し、床や壁で跳ね返ることを確認する。

## Step 4: 衝突判定と合体ロジックの実装
**目的**: 同じ色のボール同士がぶつかった際に、より大きなボールへと進化させる。
- **実装内容**:
  - Matter.jsの `collisionStart` イベントを使用してボール同士の衝突を検知。
  - 衝突した2つのボールが同じレベル（色・サイズ）であるかを判定。
  - 条件を満たす場合、元の2つのボールを削除し、衝突位置の中間点に1つ上のレベルのボールを新規生成する。
- **作成・修正するファイル**: `main.js`
- **動作確認**:
  - 同じボールが重なった際に、一回り大きい別の色のボールに変化することを確認する。

## Step 5: スコアリングと特殊処理の実装
**目的**: ボール合体時のスコア計算と、最大ボール（黒）同士の消滅処理を追加する。
- **実装内容**:
  - スコア変数の定義とUI更新処理の実装。
  - 合体成功時、新しくできたボールのレベルに応じたスコアを加算。
  - レベル8（黒）同士が衝突した場合、次のボールは生成せず、両方を削除して特大ボーナススコアを加算する。
- **作成・修正するファイル**: `main.js`, `index.html`（スコア表示部）
- **動作確認**:
  - ボールが合体するたびにスコアが増加すること、黒ボール同士で消滅しスコアが大幅に増えることを確認する。

## Step 6: ゲームサイクルの完成
**目的**: Nextボールの表示と、ゲームオーバー（デッドライン越え）の実装。
- **実装内容**:
  - 常に「次に落ちてくるボール」を決定しておき、UI上のNext表示エリアに描画・更新する。
  - デッドラインの高さを設定。
  - ボールが完全に静止したタイミング（または一定間隔）で、デッドラインを越えているボールが存在するか判定。
  - 越えている場合、ゲームオーバー画面の表示と操作の無効化（リスタートボタンの表示）。
- **作成・修正するファイル**: `main.js`, `style.css`
- **動作確認**:
  - Nextボールが正しく予告され、画面上部にボールが積み上がった際にゲームオーバー状態になることを確認する。

## Step 7: スマートフォン向け最適化と最終調整
**目的**: 実機での操作感向上とデザインのブラッシュアップ。
- **実装内容**:
  - `touchstart`, `touchmove`, `touchend` イベントを利用した操作性の向上（スワイプ中の落下位置プレビューなど）。
  - CSSによるデザインの調整（フォント、色、アニメーションなど）。
  - Matter.jsのレンダリングスタイルを非表示（またはカスタマイズ）し、HTML/CSS側の表現と合わせる（必要に応じてスプライトなどを使用）。
  - 物理挙動のパラメータ（反発係数、摩擦、重力）の微調整。
- **作成・修正するファイル**: `main.js`, `style.css`
- **動作確認**:
  - スマートフォンの実機（またはエミュレータ）でスムーズに操作でき、ゲームとして成立しているか最終確認する。