# ボール合体時の保持継続に関する調査報告書

本ドキュメントでは、ユーザーが保持（ドラッグ）しているボールが合体して別の大きなボールになった際、そのまま保持状態を継続させるための修正内容についてまとめています。

---

## 1. 現状の課題と原因の分析

### 課題
現在、ユーザーがボールをドラッグしている最中にそのボールが合体すると、ドラッグが強制的に解除され、合体後の新しいボールが落下してしまいます。

### 原因
1. **ボディの入れ替え**: Matter.js では合体時に元の2つのボディ（Bodies）を削除し、新しい1つのボディを追加します。
2. **制約（Constraint）の切断**: `MouseConstraint` は内部的に `constraint` を持っており、その `bodyB` プロパティで掴んでいる対象を管理しています。ボディが削除されると、この `bodyB` が `null` になり、物理的な結合が失われます。
3. **不完全な再代入**: 現状のコードでは `Game.mouseConstraint.body` のみを更新していますが、これだけでは Matter.js 内部の物理制約を再構築するには不十分です。

---

## 2. 変更内容（技術的な解決策）

ドラッグ状態を継続させるためには、合体直後に以下の3つのプロパティを同期的に更新する必要があります。

### 修正コード（main.js の handleCollision 内）

```javascript
// 合体後のボールを掴み状態に更新する処理
if (nextDraggedBody) {
    // 1. MouseConstraint が認識する現在のボディを更新
    Game.draggedBody = nextDraggedBody;
    Game.mouseConstraint.body = nextDraggedBody;

    // 2. 内部的な物理制約の対象 (bodyB) を新しいボディに接続
    Game.mouseConstraint.constraint.bodyB = nextDraggedBody;

    // 3. 掴み位置（オフセット）をリセット
    // これにより、合体後の新しいボールの中心がマウス/指の位置に吸い付きます
    Game.mouseConstraint.constraint.pointB = { x: 0, y: 0 };
}
```

---

## 3. スマートフォン・タッチ操作に関する検証と留意事項

スマートフォン（iPhone/Android）のブラウザでの操作において、この機能を正しく動作させるための調査結果をまとめます。

### 検証結果
- **イベントの継続性**: スマートフォンでは一度 `pointerdown`（または `touchstart`）が発生すると、指を離すまでイベントストリームが継続します。Matter.js の `MouseConstraint` はこのストリームを利用しているため、上記のように内部の `bodyB` を差し替えるだけで、タッチ操作でもスムーズに保持が継続されることを確認しました。
- **ドラッグ精度の向上**: 合体によりボールのサイズ（半径）が変化するため、`pointB`（掴んでいる相対位置）を `{ x: 0, y: 0 }` にリセットすることが重要です。これにより、ボールが大きくなっても指の真下に中心が来るようになり、操作の違和感が解消されます。

### 実装上の留意点
- **`touch-action: none` の設定**:
  `index.html` または `style.css` でゲームコンテナに対して `touch-action: none;` が設定されている必要があります。これが無いと、ブラウザの「スワイプで戻る」や「ズーム」などのネイティブ動作が優先され、ドラッグ操作が中断される原因となります。
  （※現在のプロジェクトでは既に設定されていることを確認済みです）
- **高リフレッシュレートへの対応**:
  スマートフォンの画面更新（120Hzなど）に合わせて物理演算を安定させるため、Matter.js の `Runner` が適切に動作している必要があります。

---

## 4. 修正によるユーザー体験（UX）の向上
- **直感的な操作**: 大きなボールを作る過程で手が離れないため、ゲームのテンポが損なわれません。
- **戦略性の向上**: 合体した直後の大きなボールをそのまま移動させて、次の合体を狙うといった高度なプレイが可能になります。
